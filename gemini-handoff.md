# Gemini 视频制作交接文档（纯图形版 · 无真人）

> ⚠️ 本视频**全程无真人出镜**，所有画面均为：终端模拟界面、动画架构图、代码滚动、数据表格、文字标题。不涉及任何可识别真实人物。

## 项目概述

技术排障项目。Codex CLI + DeepSeek 组合导致对话记录卡顿（每次 6 秒），根因是 Python 冷启动，通过两层缓存架构优化到 88ms。

## 目标产出

一个 **4 分 30 秒**的技术讲解视频（MP4），风格：深色背景 + 青绿色高亮 + 简约科技风。

---

## 完整分镜（全图形/动画，无真人）

### 场景 0（0:00-0:15）片头
- **画面**：纯黑背景。白色大字「Codex + DeepSeek 用户请进」从模糊到清晰淡入。副标题「你的对话记录是不是也卡到想砸键盘？」小字出现在下方。
- **音效**：键盘敲击声 + BGM 渐进
- **风格**：极简文字动画

### 场景 1（0:15-0:45）问题的呈现
- **画面**：模拟的深色终端窗口居中。终端顶部显示 `$ codex chat --history`。下方出现一个旋转的加载图标，旁边数字计时器从 0 跳动到 6.2 秒。然后界面"无响应"闪烁红色边框。重复 3 次，每次不同的对话标题。
- **风格**：像素风终端界面，暗底绿字

### 场景 2（0:45-1:15）排查：数据量
- **画面**：文件管理器风格的树形图展开：
  ```
  ~/.codex/
  ├── state_5.sqlite      [高亮黄色]
  └── sessions/
      └── *.jsonl × 11    [高亮黄色]
  ```
  然后画面切到右侧弹出统计面板：`线程: 11` `数据: 4.5 MB` `单次查询: 6秒`。问号图标放大抖动。
- **风格**：文件树 + 数据面板，暗底白字，关键数字用青绿色

### 场景 3（1:15-1:45）根因：Python 冷启动
- **画面**：左侧显示一行代码 `python -c "import sqlite3..."`。右侧是一个横向时间轴，从 0ms 到 6000ms，分五段逐段亮起：
  1. "Python 启动" ████████ 2000ms（红色）
  2. "加载 sqlite3" ███ 500ms（橙色）
  3. "SQL 查询" ███ 500ms（黄色）
  4. "JSONL 解析" ██████ 1000ms（黄色）
  5. "输出 + 退出" ████ 1000ms（橙色）
  
  总计处显示 🔴 **6000ms**。然后画面出现 ×11 的标记，总时间放大：🔴 **66 秒**（11 次查询）。
- **风格**：时间轴动画，颜色从红渐变到绿，数字跳动

### 场景 4（1:45-2:15）方案：两层架构
- **画面**：动画化的架构流程图。三个方框从左到右依次亮起并连线：
  
  **左框**（灰色）：SQLite + JSONL 图标  
  ↓ 箭头动画  
  **中框**（蓝色）：齿轮图标 + "build_cache.py（仅运行一次）"  
  ↓ 箭头动画  
  **右框**（绿色）：文件图标 + "thread_cache.json（静态缓存）"  
  
  然后下方出现新的框：  
  **下框**（亮绿色）：终端图标 + "read_history.ps1（PowerShell 原生，88ms）"  
  箭头从缓存指向下框，再指向一个用户图标（简笔画人形即可，非真实人脸）。
- **风格**：流程图动画，箭头有流动光效，各框依次亮起

### 场景 5（2:15-2:45）缓存构建演示
- **画面**：模拟终端窗口。命令提示符处逐字键入 `python build_cache.py`。回车后，下方快速滚动输出，最终停在：
  ```
  Cache built: 11 threads, 2515 total messages
  Time: 0.4s ✅
  ```
  绿色勾号闪烁。
- **风格**：终端模拟，带打字机效果

### 场景 6（2:45-3:30）查询演示（高潮）
- **画面**：终端窗口。上半部分快速敲 `rh` → 立即（无延迟）弹出 11 个线程列表。每个线程条目以青色出现在黑色终端中。计时器在角落显示 **88ms**，绿色高亮跳动。
  
  下半部分：敲 `rh 019e64be` → 305 条消息迅速滚动，计时器显示 **135ms**。
  
  画面切换：分屏对比——左侧「优化前 6000ms」红色进度条缓慢填满，右侧「优化后 88ms」绿色进度条瞬间填满。中央大字：**68×**。
- **风格**：终端操作 + 进度条对比动画，68x 需大号字体突出

### 场景 7（3:30-4:00）性能总结
- **画面**：三栏对比表，从画面底部滑入：

  | 🔴 优化前 | 🟡 第一版 | 🟢 终版 |
  |---|---|---|
  | 6000ms | 3000ms | **88ms** |

  三栏依次亮起，88ms 数字放大跳动。下方字幕：「不是优化了查询，是换了一个没有启动开销的运行时」。
- **风格**：表格动画，颜色渐变

### 场景 8（4:00-4:20）代码开源
- **画面**：GitHub 仓库页面风格界面。仓库名 `larcher-m/codex-thread-cache` 在顶部，下方文件列表滚动：`build_cache.py` `read_history.ps1` `README.md`。Star 图标闪烁。底部出现安装命令：
  ```
  git clone ... && python build_cache.py
  ```
- **风格**：模仿 GitHub 界面，暗色主题

### 场景 9（4:20-4:30）片尾
- **画面**：纯黑背景淡入。白色大字「被卡过？」→ 下方出现「👍 点个赞让更多人看到」。最底部小字 GitHub 链接。
- **BGM**：渐弱直至消失
- **风格**：极简文字，与片头呼应

---

## 全局视觉规范

| 元素 | 规范 |
|---|---|
| 背景色 | #0D1117（GitHub暗色）或纯黑 #000000 |
| 主色调 | #00FF88（青绿）用于高亮数字和成功状态 |
| 警告色 | #FF6B6B（红）用于旧方案的慢速指标 |
| 过渡色 | #FFD93D（黄）用于中间状态 |
| 代码字体 | JetBrains Mono 或 Fira Code，等宽 |
| 标题字体 | 无衬线，加粗 |
| 动画曲线 | ease-out，快速进入 + 轻微回弹 |

---

## 声音/BGM

- 全程无配音的版本：纯 BGM + 屏幕文字（用画面上的大字传达关键信息）
- 有配音的版本：按口播词生成中文男声配音，语速适中偏快
- BGM：chillhop / lofi，场景 6 起节奏稍加快

**口播词**（如有配音）：

> 如果你也把 Codex 的模型从 GPT 换成了 DeepSeek，那你大概率遇到过这个——每次翻看历史对话，点一下……等 6 秒。再点一个……又 6 秒。翻 5 个对话，半分钟就没了。
>
> 打开数据目录一看——11 个线程，4.5MB。就这？不是数据大。
>
> 真正的原因是 python -c 内联脚本。每次查询，Python 冷启动 2 秒，加载 sqlite3，连数据库，解析 JSONL……6 秒就这么没的。
>
> 解决方案：两层架构。Python 一次性读完全部数据生成 JSON 缓存——0.4 秒。日常查询用 PowerShell 原生解析——88 毫秒。自动检测过期，全自动维护。
>
> 看效果：6 秒到 88 毫秒，68 倍提升。
>
> 完整代码开源在 GitHub，两个文件加一行别名，三分钟搞定。链接在简介。有用的话，点个赞。

---

## 已发布平台

- GitHub：https://github.com/larcher-m/codex-thread-cache
- 掘金：https://juejin.cn/post/7644745124401397802
- B站专栏：https://www.bilibili.com/opus/1207468413903962150

视频发布后需在这些平台补上链接。
