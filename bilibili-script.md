# B 站视频脚本：Codex + DeepSeek 对话卡顿解决方案

> **预计时长**：4 分 30 秒
> **风格**：技术排障 + 实操演示
> **BGM**：轻电子/chillhop（后半段节奏稍快）

---

## 分镜表

| 时间段 | 画面 | 口播 | 备注 |
|---|---|---|---|
| 0:00-0:15 | 片头：黑底白字标题「Codex + DeepSeek 用户请进」，底下小字「你的对话记录是不是也卡到想砸键盘？」 | （无声，BGM 渐入） | 3-4 秒即可 |
| 0:15-0:45 | **场景1**：录屏 Codex 终端，切到一个历史对话，鼠标狂点但没有响应，加个计时器显示「6.2s」 | 「如果你也把 Codex 的模型从 GPT 换成了 DeepSeek，那你大概率遇到过这个——每次翻看历史对话，点一下……等 6 秒。再点一个……又 6 秒。你敢翻 5 个对话，半分钟就没了。我忍了一周，终于受不了，动手把它修好了。」 | 计时器可用在线秒表叠加 |
| 0:45-1:15 | **场景2**：录屏打开 `~/.codex/` 目录，高亮 `state_5.sqlite` 和 `sessions/` 文件夹。然后打开终端跑 `dir` 统计文件数 | 「第一时间我想：肯定是数据库太大了吧？打开一看——11 个线程，总共 4.5MB 数据。就这？4.5MB 的 SQLite 怎么可能要 6 秒？」 | 终端命令要提前练好 |
| 1:15-1:45 | **场景3**：画面切到 VS Code，打开一个 `python -c` 命令，逐行高亮。旁边放一个时间轴示意图 | 「真实原因藏在这里。Codex 内部查对话用的是 `python -c` 内联脚本。每次执行，你的电脑都要——启动 Python 解释器 2 秒、加载 sqlite3 0.5 秒、连接数据库 0.5 秒、再解析 JSONL 文件 1 秒。11 次查询，11 轮冷启动。不是数据大，是每次都在重新启动一个重量级运行时。」 | 这里可以做个简单的动画或标注，没有的话直接用鼠标圈也行 |
| 1:45-2:15 | **场景4**：画面展示架构图（Mermaid 图），三层结构 | 「解决方案其实很简单——既然 Python 启动慢，那就让它只启动一次。我设计了一个两层架构：底层用 Python 一次性把 SQLite 和所有 JSONL 读完，生成一个静态 JSON 缓存文件。上层日常读取，完全不用 Python，改用 PowerShell 原生解析。」 | Mermaid 架构图从 GitHub README 里截 |
| 2:15-2:45 | **场景5**：终端实操——跑 `build_cache.py`，显示 0.4 秒完成 | 「这是缓存构建脚本 `build_cache.py`，一次性读取全部数据，0.4 秒搞定。这个脚本平时不用跑——只有在对话记录有更新时才会自动触发。」 | 实际敲命令 |
| 2:45-3:30 | **场景6**：终端实操——连续敲 `rh` 多次，每次秒出。然后敲 `rh 019e64be` 查看某个线程的完整对话 | 「日常查询用的是这个 PowerShell 脚本。我给它取了个别名 `rh`。看好了——敲 `rh`，88 毫秒出列表。再看一个具体对话，135 毫秒出 305 条完整消息。6 秒到 88 毫秒，68 倍提升。」 | 这个镜头是高潮，速度要快，冲击力要强 |
| 3:30-4:00 | **场景7**：画面展示性能对比表，三列（优化前 6s、第一版 3s、终版 88ms），红色→黄色→绿色 | 「再对比一下三个阶段：原生查询 6 秒、第一版缓存 3 秒、终版 PowerShell 方案 88 毫秒。不是优化了查询，是换了一个没有启动开销的运行时。」 | 性能对比图，颜色渐变 |
| 4:00-4:20 | **场景8**：展示 GitHub 仓库页面，README 和文件列表 | 「完整代码已经开源在 GitHub，两个文件加一行别名配置，三分钟搞定。链接在视频简介和评论区。」 | |
| 4:20-4:30 | 片尾：黑底白字「如果你也被卡过，点个赞让更多人看到」，下方 GitHub 链接 | 「如果你也被这个问题折磨过，点个赞让更多 Codex + DeepSeek 用户看到。有用的话记得收藏，下期见。」 | BGM 渐出 |

---

## 逐场口播词（完整版）

### 场景 1（0:15-0:45）
> 如果你也把 Codex 的模型从 GPT 换成了 DeepSeek，那你大概率遇到过这个——每次翻看历史对话，点一下……等 6 秒。再点一个……又 6 秒。你敢翻 5 个对话，半分钟就没了。我忍了一周，终于受不了，动手把它修好了。

### 场景 2（0:45-1:15）
> 第一时间我想：肯定是数据库太大了吧？打开一看——11 个线程，总共 4.5MB 数据。就这？4.5MB 的 SQLite 怎么可能要 6 秒？

### 场景 3（1:15-1:45）
> 真实原因藏在这里。Codex 内部查对话用的是 python -c 内联脚本。每次执行，你的电脑都要——启动 Python 解释器 2 秒、加载 sqlite3 0.5 秒、连接数据库 0.5 秒、再解析 JSONL 文件 1 秒。11 次查询，11 轮冷启动。不是数据大，是每次都在重新启动一个重量级运行时。

### 场景 4（1:45-2:15）
> 解决方案其实很简单——既然 Python 启动慢，那就让它只启动一次。我设计了一个两层架构：底层用 Python 一次性把 SQLite 和所有 JSONL 读完，生成一个静态 JSON 缓存文件。上层日常读取，完全不用 Python，改用 PowerShell 原生解析。

### 场景 5（2:15-2:45）
> 这是缓存构建脚本 build_cache.py，一次性读取全部数据，0.4 秒搞定。这个脚本平时不用跑——只有在对话记录有更新时才会自动触发。

### 场景 6（2:45-3:30）
> 日常查询用的是这个 PowerShell 脚本。我给它取了个别名 rh。看好了——敲 rh，88 毫秒出列表。再看一个具体对话，135 毫秒出 305 条完整消息。6 秒到 88 毫秒，68 倍提升。

### 场景 7（3:30-4:00）
> 再对比一下三个阶段：原生查询 6 秒、第一版缓存 3 秒、终版 PowerShell 方案 88 毫秒。不是优化了查询，是换了一个没有启动开销的运行时。

### 场景 8（4:00-4:20）
> 完整代码已经开源在 GitHub，两个文件加一行别名配置，三分钟搞定。链接在视频简介和评论区。

### 片尾（4:20-4:30）
> 如果你也被这个问题折磨过，点个赞让更多 Codex + DeepSeek 用户看到。有用的话记得收藏，下期见。

---

## 录音/录制准备清单

### 终端演示（提前录好，剪进去）
1. `python build_cache.py` → 显示 0.4s 完成  
2. `rh` → 秒出列表  
3. `rh 019e64be` → 秒出 305 条消息  
4. （可选）模拟旧方案：`python -c "import sqlite3..."` → 显示耗时

### 画面素材
- GitHub 仓库截图 → `larcher-m/codex-thread-cache`
- Mermaid 架构图（从 `architecture.mermaid` 渲染或截图）
- 性能对比表（三列红黄绿渐变）
- Codex `~/.codex/` 目录截图

### 字幕/标注
- 关键数字高亮：「6 秒」「88 毫秒」「68 倍」
- 文件路径标注在画面角落
- 代码片段可以放大单独展示

---

## B 站专栏文章

视频简介里贴 B 站专栏链接，文章直接用掘金版改一下开头语就行（我会另写一篇适配 B 站风格的专栏）。视频+专栏双发，互相引流。

---

## 视频标题建议

- **封面标题**：「Codex + DeepSeek = 卡爆？一招从 6 秒干到 88 毫秒」
- **B 站标题**：「你的 Codex 切了 DeepSeek 是不是也卡到爆炸？修好了，拿去用」
- **标签**：Codex / DeepSeek / 性能优化 / 编程工具 / Python
